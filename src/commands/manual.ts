import chalk from 'chalk'

const manual = (): string => `
Create a static webserver from a directory
${chalk.cyan('https://github.com/lencse/cliserver#readme')}

Type \`cliserver serve --help\` for options
`

export default manual
